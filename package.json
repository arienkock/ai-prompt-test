{
  "name": "ai-rules-test",
  "version": "1.0.0",
  "description": "Layered architecture test app with Vue.js frontend and Express backend",
  "main": "src/main.js",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "echo 'Using static frontend build'",
    "build:backend": "echo 'Using JavaScript backend - no build needed'",
    "start": "npm run build && node src/main.js",
    "dev": "node src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.5",
    "typescript": "^5.2.2",
    "ts-node": "^10.9.2"
  },
  "author": "",
  "license": "ISC"
}
